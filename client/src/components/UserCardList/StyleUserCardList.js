import {makeStyles} from "@material-ui/core/styles";

const styleUserCardList = makeStyles((theme) => ({
    cardContent: {
        // padding: theme.spacing(8, 0, 6),
        marginTop: theme.spacing(6),
        marginBottom: theme.spacing(4),
    }
}));

export default styleUserCardList;